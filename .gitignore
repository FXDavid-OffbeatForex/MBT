# user-specific config (each user sets their own paths)
config.yaml

# generated reports
reports/*.html

# python
__pycache__/
*.pyc
.venv/
venv/

# signal CSVs — example files in examples/ are tracked; any CSV dropped in
# the root or scripts/ is personal data and should not be committed
/*.csv
/scripts/*.csv
